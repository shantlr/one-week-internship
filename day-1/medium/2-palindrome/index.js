/*
 * Exercise 2: Palindrome Checker
 *
 * Check if a string is a palindrome (reads the same forwards and backwards).
 * Ignore case and non-alphanumeric characters.
 *
 * Return true if palindrome, false otherwise.
 *
 * Examples:
 * main("radar")                        → true
 * main("hello")                        → false
 * main("A man a plan a canal Panama")  → true
 * main("Was it a car or a cat I saw")  → true
 * main("No 'x' in Nixon")              → true
 * main("race a car")                   → false
 */

function main(text) {
    // Your code here
}

module.exports = { main };
