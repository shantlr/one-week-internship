/*
 * Exercise 3: Unique Characters
 *
 * Return a string containing only the unique characters from the input,
 * preserving the order of first appearance.
 *
 * Case-sensitive: 'A' and 'a' are different characters.
 *
 * Examples:
 * main("hello")       → "helo"
 * main("aabbccabc")   → "abc"
 * main("programming") → "progamin"
 * main("abcdef")      → "abcdef"
 * main("aaaaaa")      → "a"
 * main("")            → ""
 */

function main(input) {
    // Your code here
}

module.exports = { main };
