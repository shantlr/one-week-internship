/*
 * Exercise 2: Word Count
 *
 * Count the occurrences of each word in a string.
 * Return an object where keys are words and values are counts.
 * Words are case-insensitive (convert to lowercase).
 * Ignore punctuation.
 *
 * Examples:
 * main("hello world")           → { hello: 1, world: 1 }
 * main("the cat and the dog")   → { the: 2, cat: 1, and: 1, dog: 1 }
 * main("Hello, hello, HELLO!")  → { hello: 3 }
 * main("")                      → {}
 */

function main(text) {
    // Your code here
}

module.exports = { main };
